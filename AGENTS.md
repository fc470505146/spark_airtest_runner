# 项目执行规范（长期生效）

本文件用于约束本仓库内的任务执行方式。除非用户在当前任务中明确覆盖，默认始终遵守以下原则。

## 语言与输出约定
- 所有回复、说明、注释、文档使用简体中文。
- 代码内容（标识符、语法、代码实现）使用英文。
- 在非代码与非专有名词场景下使用中文。
- git提交需要使用中文
- markdown文档名称需要使用中文

# 编程原则
用于减少 LLM 在编码中常见错误的行为准则。可根据需要与项目专属指令合并使用。
权衡：这些准则更偏向谨慎，而不是速度。对于非常简单的任务，请自行判断。

## 1. 编码前先思考

不要假设。不要隐藏困惑。明确说明取舍。

在实现之前：

- 明确说明你的假设。如果不确定，就提问。
- 如果存在多种理解方式，就把它们列出来，不要默默选择一种。
- 如果存在更简单的方法，就说出来。必要时要提出反对意见。
- 如果有不清楚的地方，就停下来。指出哪里让你困惑，并提问。
- 在写代码前，先识别系统中必须始终为真的事实、不允许出现的非法状态，以及这些约束生效的边界位置。
- 不要只按功能流程堆代码；关键约束不能只依赖注释、约定或人的小心。

## 2. 简单优先

用最少的代码解决问题。不要做投机性的设计。

- 不要添加超出需求的功能。
- 不要为只使用一次的代码做抽象。
- 不要添加需求中没有要求的“灵活性”或“可配置性”。
- 不要为不可能发生的场景写错误处理。
- 如果你写了 200 行代码，而其实 50 行就能解决，就重写。
- 问自己：“一位资深工程师会不会觉得这太复杂了？”如果会，就简化。
- 当多种方案都能完成功能时，优先选择长期更稳定、更不易破坏约束、更不容易产生语义漂移的方向。

## 3. 外科手术式修改

只改必须改的地方。只清理你自己造成的问题。

在编辑已有代码时：

- 不要“顺手改进”旁边的代码、注释或格式。
- 不要重构没有出问题的部分。
- 遵循现有代码风格，即使你自己会用不同写法。
- 如果发现无关的废弃代码，提出来，但不要删除。

当你的修改产生了孤立代码时：

- 移除因为你的修改而变得未使用的 import、变量或函数。
- 不要删除原本就存在的废弃代码，除非用户明确要求。

检验标准是：每一行修改都应该能直接对应到用户的请求。

## 4. 目标驱动执行

定义成功标准。持续迭代，直到验证通过。

把任务转化为可验证的目标：

- “添加校验” → “先为非法输入写测试，再让测试通过”
- “修复 bug” → “先写一个能复现 bug 的测试，再让测试通过”
- “重构 X” → “确保重构前后测试都能通过”

对于多步骤任务，先给出一个简短计划：

1. 【步骤】→ 验证：【检查方式】
2. 【步骤】→ 验证：【检查方式】
3. 【步骤】→ 验证：【检查方式】

强成功标准可以让你独立循环推进。弱成功标准，例如“让它能用”，则需要不断澄清。

## 5. 维护不变量

编程的本质是在约束下维护不变量。实现应围绕系统约束展开，而不是只完成表面功能。

- 优先选择能清晰表达约束、缩小状态空间、尽早暴露错误、并从结构上减少非法状态的实现。
- 数据结构、类型、构造过程、接口、planner、runtime、测试与错误边界，都应服务于不变量的维护。
- 不要让非法状态轻易进入系统。
- 不要把关键约束分散在多个调用点里。
- 如果某个约束必须始终成立，应尽量让代码结构保证它，而不是依赖调用者记住它。

## 6. 单一事实来源

同一语义或规则只能在一处定义，其他位置只引用，不重复解释。

- 禁止并行维护多套等价逻辑。
- 避免复制粘贴规则、条件判断、状态映射或业务语义。
- 如果多个地方需要同一规则，应提取为一个明确的来源。
- 当发现语义漂移风险时，优先收敛定义，而不是继续补丁式修复。
- 不要为了局部方便制造第二套“差不多一样”的逻辑。

## 7. Fast-Fail

关键输入、关键依赖、关键字段缺失时，必须立即报错并中止流程。

- 禁止静默降级。
- 禁止隐式回退。
- 禁止吞异常后继续执行。
- 不要用默认值掩盖缺失的关键数据。
- 错误应尽早暴露在边界处，而不是在系统深处变成更难排查的问题。
- 对非关键路径可以宽容，但必须明确区分关键失败与可恢复失败。

## 8. 优先级规则

当这些准则之间出现冲突时，按以下顺序处理：

1. 用户明确要求优先。
2. 正确性与不变量优先。
3. 外科手术式修改优先。
4. 简单实现优先。
5. 长期可维护性优先。
6. 速度最后。

如果这些准则生效，表现应该是：diff 中更少出现不必要的修改，更少因为过度复杂而返工，更少语义漂移和重复逻辑，并且澄清问题会发生在实现之前，而不是犯错之后。
